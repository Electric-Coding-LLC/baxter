name: Release Candidate

on:
  workflow_dispatch:
    inputs:
      version:
        description: "RC version (example: v0.4.0-rc1)"
        required: true
        default: "v0.4.0-rc1"

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

jobs:
  build-rc:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-go@v6
        with:
          go-version: "1.25.8"
          cache: true
      - run: go mod download
      - name: Validate RC version format
        run: |
          set -euo pipefail
          VERSION='${{ inputs.version }}'
          if ! [[ "$VERSION" =~ ^v[0-9]+\.[0-9]+\.[0-9]+-rc[0-9]+$ ]]; then
            echo "Invalid RC version format: $VERSION"
            echo "Expected format: vX.Y.Z-rcN"
            exit 1
          fi
      - run: go install golang.org/x/vuln/cmd/govulncheck@latest
      - run: govulncheck ./...
      - run: go test ./...
      - name: Build RC artifacts
        env:
          BAXTER_RELEASE_SKIP_TESTS: "1"
        run: ./scripts/release.sh '${{ inputs.version }}'
      - name: Upload RC artifacts
        uses: actions/upload-artifact@v7
        with:
          name: baxter-${{ inputs.version }}
          path: dist/${{ inputs.version }}
          if-no-files-found: error
          retention-days: 14

  build-rc-macos-app:
    runs-on: [self-hosted, macOS, ARM64, baxter-macos26]
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v5
      - name: Package BaxterApp RC artifact
        run: ./scripts/package-macos-app.sh --output-dir "dist/${{ inputs.version }}"
      - name: Upload RC app artifact
        uses: actions/upload-artifact@v7
        with:
          name: baxter-app-${{ inputs.version }}
          path: dist/${{ inputs.version }}/Baxter-darwin-arm64.zip
          if-no-files-found: error
          retention-days: 14

  validate-rc-artifacts:
    runs-on: [self-hosted, macOS, ARM64, baxter-macos26]
    needs: [build-rc, build-rc-macos-app]
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v5
      - name: Download RC artifacts
        uses: actions/download-artifact@v8
        with:
          name: baxter-${{ inputs.version }}
          path: .ci/rc-artifacts
      - name: Download RC app artifact
        uses: actions/download-artifact@v8
        with:
          name: baxter-app-${{ inputs.version }}
          path: .ci/rc-app-artifact
      - name: Validate RC app artifact unzip
        run: |
          set -euo pipefail
          unzip -q .ci/rc-app-artifact/Baxter-darwin-arm64.zip -d .ci/rc-app-unpacked
          test -d .ci/rc-app-unpacked/Baxter.app
      - name: Validate RC install/upgrade/rollback path
        run: |
          set -euo pipefail
          EVIDENCE_PATH="$GITHUB_WORKSPACE/.ci/rc-validation-evidence.md"
          SUMMARY_PATH="$GITHUB_WORKSPACE/.ci/rc-validation-summary.json"
          ./scripts/rc-artifact-validation.sh \
            --artifact-dir "$GITHUB_WORKSPACE/.ci/rc-artifacts" \
            --version '${{ inputs.version }}' \
            --evidence "$EVIDENCE_PATH" \
            --summary-json "$SUMMARY_PATH"
      - name: Write RC validation step summary
        if: always()
        run: |
          set -euo pipefail
          SUMMARY_PATH="$GITHUB_WORKSPACE/.ci/rc-validation-summary.json"
          SUMMARY_MD_PATH="$GITHUB_WORKSPACE/.ci/rc-validation-summary.md"
          if [ ! -f "$SUMMARY_PATH" ]; then
            echo "## RC Validation Summary" >>"$GITHUB_STEP_SUMMARY"
            echo >>"$GITHUB_STEP_SUMMARY"
            echo "- overall result: \`missing\`" >>"$GITHUB_STEP_SUMMARY"
            exit 0
          fi

          bash ./scripts/rc-validation-summary.sh \
            --summary-json "$SUMMARY_PATH" \
            --output "$SUMMARY_MD_PATH"

          cat "$SUMMARY_MD_PATH" >>"$GITHUB_STEP_SUMMARY"
          {
            echo
            echo "- workflow run: [${{ github.run_id }}](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }})"
            echo "- evidence artifact: \`rc-validation-evidence-${{ inputs.version }}-${{ github.run_id }}\`"
          } >>"$GITHUB_STEP_SUMMARY"
      - name: Collect launchd logs (on failure)
        if: failure()
        run: |
          set -euo pipefail
          mkdir -p .ci/launchd-logs
          cp -f "$HOME/Library/Logs/baxterd.out.log" .ci/launchd-logs/ 2>/dev/null || true
          cp -f "$HOME/Library/Logs/baxterd.err.log" .ci/launchd-logs/ 2>/dev/null || true
      - name: Upload launchd logs (on failure)
        if: failure()
        uses: actions/upload-artifact@v7
        with:
          name: rc-launchd-logs-${{ github.run_id }}
          path: .ci/launchd-logs
          if-no-files-found: warn
      - name: Upload RC validation evidence
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: rc-validation-evidence-${{ inputs.version }}-${{ github.run_id }}
          path: |
            .ci/rc-validation-evidence.md
            .ci/rc-validation-summary.json
            .ci/rc-validation-summary.md
          if-no-files-found: warn
          retention-days: 14
