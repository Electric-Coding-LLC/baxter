backup_roots = [
  "/Users/you/Documents",
  "/Users/you/Pictures"
]

# daily | weekly | manual
schedule = "daily"

[s3]
# Leave bucket empty to use local object storage only.
# Set bucket+region to enable S3 object storage.
# Leave endpoint empty for AWS S3. Use a full https URL for S3-compatible providers.
endpoint = ""
region = ""
bucket = ""
prefix = "baxter/"

[encryption]
# Passphrase-derived key stored in Keychain.
keychain_service = "baxter"
keychain_account = "default"
